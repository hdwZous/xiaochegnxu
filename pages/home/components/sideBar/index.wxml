<view 
    bindtap="formSubmit" 
    class=" home_sidebar sign {{sideBarHide ? 'hideActive' : ''}}" 
    data-user-action="{{data.userAction}}" 
    data-trace-id="{{pageDataTraceId}}"
    data-page-id="{{buriedObj.pageIdFirstPage}}"
    data-current-page-name="{{buriedObj.currentPageName}}"
    data-pre-page-name='{{buriedObj.prePageName}}'
    aria-hidden="true">
    <image aria-hidden="true" lazy-load="true" src="{{data.imgUrL}}" class="img"></image>
</view>