<view class="store_list_box">
  <!-- <block wx:if="{{hookLazy}}"> -->
    <!-- <store-list
      traceId="{{traceId}}"
      item="{{info}}"
      pageId="{{recommendObj.pageIdFirstPage}}"
      currentPageName="{{recommendObj.currentPageName}}"
      prePageName="{{recommendObj.prePageName}}"
      recommendObj="{{recommendObj}}"
    ></store-list> -->
    <store-item
      item="{{info}}" 
      buriedObj="{{recommendObj}}"
      traceId="{{traceId}}"
      noNeedExposure="{{true}}"
    ></store-item>
  <!-- </block>
  <block wx:else>
    <view class="cp-lazy-skeleton" style="height: 296rpx"></view>
  </block> -->
</view>