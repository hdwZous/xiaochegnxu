<view class="getCustomers">
    <view class="header">
        <view class="inputPage">
            <image src="https://storage.360buyimg.com/wximg/getCustomers/getCustomer.png" alt="京东到家" style="width:{{idolImage.width}}px; height: {{idolImage.height}}px;"></image>
        </view>
    </view>
    <view class="formContant">
        <view class="inputPage" wx:if="{{inputGroupShow}}">
            <view class="position">
                <view class="positionBtn">
                    <image src="https://storage.360buyimg.com/wximg/getCustomers/positionIcon.png"></image>
                    <view class="text">{{options.cityName}}</view>
                </view>
            </view>
            <view class="formGroup">
                <view class="welcomeCode">
                    <input  type="text" placeholder="点击输入您的邀请码" value="{{options.code}}" disabled="true"/>
                </view>
                <view class="phoneNum">
                    <input maxlength="11" type="number" placeholder="点此输入您的手机号" value="{{phone}}" bindinput="setPhoneData"/>
                </view>
                <view class="getTicket {{canReceive? '':'noReceive'}}" bindtap="getTicket">
                   <text>立即领取</text>
                </view>
            </view>
            <view class="footerMsg">
                <view class="title">领券关注门店</view>
                <view class="part1">
                    1、活动期间新人券包仅限新用户领取，且同一用户只能领取一次，同一账户，同一手机号，同一终端设备号，均视为同一用户;
                </view>
                <view class="part2">2、使用地区仅限开通了京东到家的城市;</view>
                <view class="part3">3、领取成功后，优惠券将放至您输入的手机号账户中; </view>
                <view class="part4">4、优惠券不能以任何现金形式进行兑换;</view>
                <view class="part5">5、如有问题请联系在线客服。</view>
            </view>
        </view>
        <view class="couponGroupPage" wx:if="{{couponShow}}">
            <view class="couponGroup">
                <view class="title">优惠券已放入<view class="phone">{{phone}}</view>所绑定的账户内</view>
                <view class="couponContent" wx:for="{{result}}" wx:for-index="idx" wx:for-item="item" wx:key="idx">
                    <view class="number flex-row">
                        <text>{{item.amount}}</text>
                        <text class="unit">{{item.amountUnit}}</text>
                    </view>
                    <view class="message flex-auto">
                        <view class="radio_top"></view>
                        <view class="radio_bottom"></view>
                        <view class="dec">{{item.couponTitle}}</view>
                        <view class="details"><text>{{item.couponTypeDesc}}</text><text>{{item.couponTypeDesc}}</text></view>
                        <view class="date">{{item.avilableDate}}</view>
                    </view>
                </view>
            </view>
            <view class="getTicket useNow" bindtap="toHome">
                <text>立即使用</text>
            </view>
            <view class="noceMore" bindtap="noceMore">
                换个手机号再领一张>>
            </view>
        </view>
    </view>
</view>