<block wx:if="{{$taroCompReady}}">
    <view class="{{styles.winnerShareProduct}}">
        <picture-download compid="{{$compid__25}}"></picture-download>
        <block wx:if="{{params.taskId && params.storeId}}">
            <view class="{{styles.header}}">
                <view class="{{styles.mainHeader}}">
                    <view class="{{styles.storeName}}">
                        <image src="https://image3.imdada.cn/5596ea6e4048461b80109be0652a0c09.png" class="{{styles.storeIcon}}"></image>{{params.storeName}}</view>
                    <view class="{{styles.searchBox}}">
                        <image src="https://image3.imdada.cn/2828828c5c6a4c7e94e19ae6be07995b.png" class="{{styles.searchIcon}}"></image>
                        <input confirm-type="search" class="{{styles.searchInput}}" placeholder="请搜索店内商品" placeholder-style="color:#c8c8c8" disabled="{{true}}" bindtap="anonymousFunc0" />
                    </view>
                </view>
            </view>
        </block>
        <block>
            <block wx:if="{{params.taskId}}">
                <block>
                    <block wx:if="{{params.storeId}}">
                        <block>
                            <block wx:if="{{!loading && categories.length < 1}}">
                                <not-fund compid="{{$compid__26}}"></not-fund>
                            </block>
                            <block wx:else>
                                <main compid="{{$compid__27}}"></main>
                            </block>
                        </block>
                    </block>
                    <block wx:else>
                        <not-fund compid="{{$compid__28}}"></not-fund>
                    </block>
                </block>
            </block>
            <block wx:else>
                <not-fund compid="{{$compid__29}}"></not-fund>
            </block>
        </block>
        <view class="{{styles.flab}}" bindtap="anonymousFunc1"></view>
    </view>
</block>